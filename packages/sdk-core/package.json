{
  "name": "@ziviola/sdk-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared SDK core (private, bundled into product packages)",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --emitDeclarationOnly --outDir dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "undici": "^6.19.0"
  }
}
